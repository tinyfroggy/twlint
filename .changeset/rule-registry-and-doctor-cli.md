---
"twlinter": minor
---

Unify every rule behind a shared registry with on/off and severity config for
both the CLI and the Oxlint/ESLint plugin, add `twlinter --doctor`, `--explain`,
and `--rules`, and expose all rules through the plugin.
